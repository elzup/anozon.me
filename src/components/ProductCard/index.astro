---
import type { Product } from '../../types'
export interface Props {
	product: Product
}
const { product } = Astro.props
const size = product.size ?? 'M'
const hasImage = product.filename && product.filename !== '.png'
---

<a
	href={product.url}
	target={'_blank'}
	data-disabled={!product.urlLive}
	data-size={size}
	rel="noopener noreferrer"
>
	<div class="card">
		<div class="img-wrap">
			{hasImage ? (
				<img src={`/static/${product.filename}`} alt={product.title} />
			) : (
				<div class="placeholder">
					<span>{product.title.slice(0, 2)}</span>
				</div>
			)}
		</div>
		<h4>{product.title}</h4>
		<p>{product.description}</p>
	</div>
</a>

<style>
	@keyframes hilight {
		0% {
			background: white;
		}
		100% {
			background: #eee;
		}
	}

	:root > a {
		position: relative;
		&:before {
			position: absolute;
			top: 0.5em;
			left: -0.5em;
			content: '⬢';
		}
		&:after {
			position: absolute;
			bottom: -0.5em;
			right: -0.5em;
			content: '⬢';
		}
		&[data-disabled='true'] {
			pointer-events: none;
			cursor: none;
		}
	}

	.card {
		width: 300px;
		text-align: center;
		background: #ffffffb0;
		margin-top: 20px;
		border: dashed #dcdcdc 1px;
		padding: 5px;
		&:hover {
			animation: hilight 0.5s ease 0s forwards;
		}
	}

	.img-wrap {
		height: 200px;
	}

	[data-size='S'] .card {
		width: 200px;
	}
	[data-size='S'] .img-wrap {
		height: 130px;
	}

	[data-size='L'] .card {
		width: 420px;
	}
	[data-size='L'] .img-wrap {
		height: 280px;
	}

	img {
		width: 95%;
		height: 95%;
		object-fit: contain;
		border-radius: 5px;
	}

	.placeholder {
		width: 100%;
		height: 100%;
		display: flex;
		align-items: center;
		justify-content: center;
		background: linear-gradient(135deg, #e0e0e0 0%, #c8c8c8 100%);
		border-radius: 5px;
	}
	.placeholder span {
		font-size: 3rem;
		color: #888;
		font-weight: bold;
	}
</style>
